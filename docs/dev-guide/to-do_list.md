# To-Do List
- Add retry button if nomiplace fails to fetch results or timeout
- Add retry button if backend fails to fetch results or timeout
- During loading, prevent the back feature from working 
- Add edge colors to jeepney lines