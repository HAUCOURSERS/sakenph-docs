# Tracking User Current GeoLocation

## Compass Direction
> Upon initialization of MapWidget, the compass listener is then enabled. During the app's runtime, it will actively update
> userCompassRotation from MapHelperProvider provider.

